export declare const templatesList: readonly ["orion", "legacy", "palette", "ignite", "joyful", "professional", "classic", "simple", "designer", "majestic", "horizon", "elevate", "naruto", "nurture", "eternal", "innovate", "intuify", "pixel", "essence", "genesis", "nexus", "zenith", "pinnacle", "aurora", "maven", "vintage", "sleek", "ethereal", "equinox", "azurill", "bronzor", "chikorita", "developerx", "ditto", "gengar", "glalie", "kakuna", "leafish", "nosepass", "onyx", "pikachu", "rhyhorn"];
export declare const layoutsList: {
    orion: string[][][];
    legacy: string[][][];
    palette: string[][][];
    ignite: string[][][];
    joyful: string[][][];
    professional: string[][][];
    classic: string[][][];
    simple: string[][][];
    designer: string[][][];
    majestic: string[][][];
    horizon: string[][][];
    elevate: string[][][];
    naruto: string[][][];
    nurture: string[][][];
    eternal: string[][][];
    innovate: string[][][];
    pixel: string[][][];
    essence: string[][][];
    intuify: string[][][];
    genesis: string[][][];
    nexus: string[][][];
    zenith: string[][][];
    pinnacle: string[][][];
    aurora: string[][][];
    maven: string[][][];
    vintage: string[][][];
    sleek: string[][][];
    ethereal: string[][][];
    equinox: string[][][];
    azurill: string[][][];
    bronzor: string[][][];
    chikorita: string[][][];
    developerx: string[][][];
    ditto: string[][][];
    gengar: string[][][];
    glalie: string[][][];
    kakuna: string[][][];
    leafish: string[][][];
    nosepass: string[][][];
    onyx: string[][][];
    pikachu: string[][][];
    rhyhorn: string[][][];
};
export declare const colorList: {
    orion: string;
    legacy: string;
    palette: string;
    joyful: string;
    ignite: string;
    professional: string;
    classic: string;
    simple: string;
    designer: string;
    majestic: string;
    eternal: string;
    naruto: string;
    nurture: string;
    elevate: string;
    innovate: string;
    horizon: string;
    pixel: string;
    essence: string;
    intuify: string;
    genesis: string;
    nexus: string;
    zenith: string;
    pinnacle: string;
    aurora: string;
    maven: string;
    vintage: string;
    sleek: string;
    ethereal: string;
    equinox: string;
    azurill: string;
    bronzor: string;
    chikorita: string;
    developerx: string;
    ditto: string;
    gengar: string;
    glalie: string;
    kakuna: string;
    leafish: string;
    nosepass: string;
    onyx: string;
    pikachu: string;
    rhyhorn: string;
};
export type Template = (typeof templatesList)[number];
